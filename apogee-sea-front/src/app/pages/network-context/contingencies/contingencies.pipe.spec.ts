import {ContingenciesPipe} from './contingencies.pipe';

describe('ContingenciesPipe', () => {
  it('create an instance', () => {
    const pipe = new ContingenciesPipe();
    expect(pipe).toBeTruthy();
  });
});
